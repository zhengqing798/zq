# RAG 知识库入库说明（任务10 · 步骤1）

| 项目 | 内容 |
|------|------|
| 向量库 | **FAISS**（`IndexFlatIP` + L2 归一化向量 = 余弦相似度）｜ 向量落盘 `data/rag/jobs_kb_vectors.npy`，**索引在检索端加载时内存重建**（8,852 × 512 重建 <0.1s） |
| Embedding | **BAAI/bge-small-zh-v1.5**（512 维，[CLS] 向量 + L2 归一化，CPU 推理；模型经 hf-mirror 下载） |
| 卡片总数 | 8852（岗位卡 8836 + 结论卡 16） |
| 向量矩阵 | (8852, 512) ｜ 向量化耗时 0s |
| 卡片明细 | `data/processed/RAG_知识卡.csv`（可逐条审计）｜ 元数据 `data/rag/jobs_kb_cards.json` |

---

## 一、卡片设计

**岗位卡**（每岗位一张，共 8836 张）：把结构化字段拼成一句可读文本，字段顺序固定——

> `岗位：…。公司：…。城市：…。薪资：…（下限-上限 元/月）。经验要求：…。学历要求：…。技能标签：…。职位描述摘要：…（前 160 字）`

**结论卡**（16 张）：把任务4/5/6/7 的关键结论固化成知识卡，**每张都标注来源**（对应报告章节/图号），用于回答「薪资多少」「哪些技能最热」「聚类有几类」这类统计型问题——这类问题靠岗位卡检索效率低，直接命中结论卡更准。

结论卡覆盖：薪资分位与经验-薪资曲线、经验结构、学历门槛、技能热度、企业集中度、招聘活跃度、地域分布、匹配权重与性能、评分模型指标、聚类画像（含二阶细分）、多模型对比、数据质量。

## 二、检索自检

```
  厦门 Java 开发 3-5 年         → Java开发工程师（ERP业务）(厦门,0.69)；Java开发工程师(厦门,0.69)；JAVA开发工程师-厦门(厦门,0.69)；ERP 系统开发工程师（前端、后端）(厦门,0.69)；ERP系统高级开发工程师（SAP方向）(厦门,0.69)
  苏州 大专 质量工程师              → 质量工程师(苏州,0.74)；质量工程师(苏州,0.73)；pqe质量工程师(苏州,0.72)；质量工程师(苏州,0.72)；QE质量工程师(苏州,0.72)
  薪资 2 万以上的算法岗             → 定位算法工程师(湖州,0.75)；算法工程师(福州,0.75)；算法工程师(湖州,0.74)；算法测试工程师(苏州,0.74)；算法工程师(苏州,0.74)
  福州 前端 Vue                → 前端（急招2人/线上面试/福州办公哦）(厦门,0.66)；web前端开发工程师(福州,0.66)；中高级Java全栈开发工程师(福州,0.65)；中高级前端开发工程师(福州,0.64)；全栈工程师（AI方向）-福州(福州,0.63)
  经验不限 测试                  → 测试经理(厦门,0.61)；测试工程师（接受无经验，可培养）(宁波,0.61)；测试主管/测试经理(厦门,0.60)；检验员(湖州,0.60)；功能测试（初级）(嘉兴,0.60)
```

## 三、为什么选 FAISS + 本地 BGE

| 选择 | 理由 |
|---|---|
| **FAISS** | ① 课程列出的候选向量库之一（Chroma/FAISS/Milvus）；② `IndexFlatIP` + 归一化向量即**精确余弦检索**，无需训练索引；③ 8,852 条规模下内存索引重建 <0.1s |
| **本地 BGE-small-zh-v1.5** | ① **DeepSeek 官方不提供 embedding 接口**，必须另找嵌入模型；② 中文效果好、模型约 95MB、512 维，CPU 可跑；③ 数据不出本机（岗位数据无需上传第三方） |
| 不用 Chroma | 先试 Chroma 1.5.9（本地持久化），**跨进程重新加载时报 `Error loading hnsw index`**，故换 FAISS |
| FAISS 索引不落盘 | **FAISS 的 C++ 层在 Windows 上无法打开中文路径**（本项目目录含中文），`faiss.write_index` 直接失败；因此向量用 `.npy` 落盘、索引启动时内存重建（代价 <0.1s） |

## 四、局限与如实说明

| # | 说明 |
|---|---|
| 1 | 岗位卡把长职位描述截断到 160 字（控制向量质量与耗时），**长尾细节会丢**；如需精确匹配可在回答阶段回查原始 CSV |
| 2 | 结论卡是**人工整理**的报告结论，不是模型自动生成——好处是数字可溯源，坏处是覆盖面取决于整理者 |
| 3 | 检索是**纯语义**的（FAISS 无内建元数据过滤），城市/薪资等结构化条件由 `query.py` 的 `city` 过滤与 `structured_filter()` 补齐；召回评估对比了这两种模式 |
| 4 | 简历数据（500 份）**未入知识库**：本任务的问答场景是「岗位/市场」，简历侧用匹配工具处理更合适 |

> 复现：`python src/rag/build_kb.py`（首次会从 hf-mirror 下载模型，约 95MB）
