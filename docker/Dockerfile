# 任务12 Docker 部署
# TODO(2026-09-22)：分层编写 —— 基础镜像(依赖缓存层) → 复制 src → 启动命令
# 示例（正式实现时按需调整）：
# FROM python:3.12-slim
# WORKDIR /app
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# CMD ["uvicorn", "src.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
