"""岗位-简历人岗匹配推荐系统 —— 源码包。

按任务分模块：crawler(任务2) / preprocessing(任务3) / analysis(任务4) /
models.scoring|matching|clustering(任务5-8) / rag|agent(任务10) /
api|web(任务11)。

用法（示例）：
    from src.preprocessing.cleaner import clean_jobs
"""
